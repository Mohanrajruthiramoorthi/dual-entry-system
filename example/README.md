# Example

This folder contains a minimal `example/App.js` demonstrating usage of `DualEntry`.

How to run:

1. From your React Native app that consumes this package, import the `ExampleApp` or copy `example/App.js` into your app.
2. Make sure `react-native-paper` and its peer deps are installed and configured.
